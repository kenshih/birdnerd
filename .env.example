# Project-root .env — values passed only by npm run dev:local-google.
# Other local commands ignore this file's Google values; never use VITE_* names.
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID="…"
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET="…"
