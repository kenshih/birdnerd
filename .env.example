# Project-root .env — both values required only by npm run dev:local-google.
# Local tooling ignores ambient Google credentials; never use VITE_* names.
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID="…"
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET="…"
